APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
# Match the public URL (include /backend/public if the site is in a cPanel subfolder).
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173
# Required when the SPA is on another domain than this API (VITE_API_URL points here).
# Comma-separated, no spaces. Example: shop on acelogstores, API on host.hotelbiza:
# CORS_ALLOWED_ORIGINS=https://acelogstores.online,https://www.acelogstores.online,https://host.hotelbiza.online,http://127.0.0.1:8080,http://localhost:8080
CORS_ALLOWED_ORIGINS=https://acelogstores.online,https://www.acelogstores.online,https://host.hotelbiza.online,http://127.0.0.1:8080,http://localhost:8080

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=acelog
DB_USERNAME=root
DB_PASSWORD=

# SprintPay (for virtual accounts and webhook)
# WEBKEY=your_sprintpay_webkey
# SPRINTPAY_API_KEY=
# SPRINTPAY_SECRET=
# Required for POST /api/webhooks/sprintpay — must match X-Auth-Token or X-Webhook-Secret or Bearer token
# SPRINTPAY_WEBHOOK_SECRET=

# VTU / bills (SprintPay partner API → VTpass). See docs/VTU_SPRINTPAY.md
# SPRINTPAY_API_BASE=https://web.sprintpay.online/api
# SPRINTPAY_API_TOKEN=   # optional; defaults to SPRINTPAY_WEBHOOK_SECRET for merchant/vas auth
# SPRINTPAY_VTU_ENABLED=false
# SPRINTPAY_VTU_MOCK=false
# Optional path overrides if Readme differs:
# SPRINTPAY_VTU_PATH_AIRTIME=merchant/vas/buy-ng-airtime
# SPRINTPAY_VTU_PATH_DATA=merchant/vas/buy-data
# SPRINTPAY_VTU_PATH_CABLE_VALIDATE=merchant/vas/validate-cable
# SPRINTPAY_VTU_PATH_CABLE_BUY=merchant/vas/buy-cable
# SPRINTPAY_VTU_PATH_ELECTRICITY_VALIDATE=merchant/vas/validate-electricity-meter
# SPRINTPAY_VTU_PATH_ELECTRICITY_BUY=merchant/vas/buy-electricity
# SPRINTPAY_CATALOG_PATH_GET_DATA=get-data
# SPRINTPAY_CATALOG_PATH_GET_DATA_VARIATIONS=get-data-variations
# SPRINTPAY_CATALOG_PATH_CABLE_PLANS=cable-plan
# SPRINTPAY_CATALOG_PATH_ELECTRICITY_VARIATIONS=get-electricity-variations

# Media storage target for category/product uploads (put inside mini-laravel):
# MEDIA_STORAGE_ROOT=/absolute/path/to/mini-laravel/public/storage
# Optional absolute URL prefix for uploaded media (example: https://web.sprintpay.online)
# MEDIA_PUBLIC_BASE_URL=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
