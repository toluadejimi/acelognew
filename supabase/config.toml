project_id = "wwbjdesytvzwoewkzghx"

# JWT not verified at gateway; we validate X-User-Token inside the function
[functions.generate-virtual-account]
verify_jwt = false
entrypoint = "./functions/generate-virtual-account/index.ts"

[functions.sprintpay-webhook]
enabled = true
verify_jwt = false
import_map = "./functions/sprintpay-webhook/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/sprintpay-webhook/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/sprintpay-webhook/*.html" ]
